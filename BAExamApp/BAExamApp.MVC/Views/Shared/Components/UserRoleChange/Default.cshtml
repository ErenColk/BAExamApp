@model IList<string>
@if (Model.Contains("Admin") && Model.Contains("Trainer"))
{

<div class="d-flex align-items-center ms-1 ms-lg-3" id="kt_header_user_menu_toggle">
    <div class="cursor-pointer symbol symbol-30px symbol-md-40px"
         data-kt-menu-trigger="click" data-kt-menu-attach="parent"
         data-kt-menu-placement="bottom-end">
            <button>@SharedLocalizer["Change_Role"]</button>
    </div>
    <div class="menu menu-sub menu-sub-dropdown menu-column menu-rounded menu-gray-800 menu-state-bg menu-state-primary fw-bold py-4 fs-6 w-275px"
         data-kt-menu="true">
        <div class="menu-item px-3">
            <div class="menu-content d-flex align-items-center px-3">
                <div class="d-flex flex-column">
                    @if (Model != null)
                    {
                        foreach (var role in Model)
                        {
                            <div class="fw-bolder d-flex align-items-center fs-5">
                                <a class="fw-bold text-muted text-hover-primary fs-7" asp-area="@role.ToString()" asp-controller="Home" asp-action="Index">@role</a>
                            </div>
                        }
                    }
                </div>
            </div>
        </div>
    </div>
</div>
}